# 数据ETL场景
## 业务库同步到数仓STG层
  - 选择要同步的数据源，数据库，和数据表
  - 指定数仓表，若表不存在则创建新表，
  - 选在要同步的列字段
  - 配置数据同步任务
  - 发布（启用）任务

## 数仓STG层同步到数仓ODS层
  - 选择stg表
  - 指定ods表，若表不存在则创建新表
  - 配置数据同步任务
  - 发布（启用）任务

## 数仓ODS层同步到数仓DW层
  - 编写数据抽取SQL
  - 配置数据同步任务
  - 发布（启用）任务

## 数仓DW层同步到数仓ADS层
  
