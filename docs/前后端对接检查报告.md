# ETL系统前后端对接检查报告

生成时间: 2026-03-16
检查范围: 后端API接口 vs 前端Views实现

## 一、后端API接口清单

### 1. ETL任务管理 (ETLTaskViewSet)
**路由前缀**: `/api/dataetl/tasks`

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/` | GET | 获取任务列表（支持筛选） | ✅ 已实现 |
| `/` | POST | 创建新任务 | ✅ 已实现 |
| `/{id}` | GET | 获取任务详情 | ✅ 已实现 |
| `/{id}` | PUT | 更新任务 | ✅ 已实现 |
| `/{id}` | DELETE | 删除任务 | ✅ 已实现 |
| `/simple` | GET | 获取简单列表（下拉框） | ✅ 已实现 |
| `/{id}/execute` | POST | 执行任务 | ✅ 已实现 |
| `/{id}/create-version` | POST | 创建版本快照 | ✅ 已实现 |
| `/{id}/versions` | GET | 获取任务版本列表 | ✅ 已实现 |
| `/{id}/rollback` | POST | 回滚到指定版本 | ✅ 已实现 |
| `/{id}/validate-config` | POST | 验证DataX配置 | ✅ 已实现 |
| `/{id}/datx-config` | GET | 生成DataX配置 | ✅ 已实现 |
| `/{id}/execute-dry-run` | POST | 模拟执行 | ✅ 已实现 |
| `/from-template` | POST | 从模板创建任务 | ✅ 已实现 |
| `/{id}/clone` | POST | 克隆任务 | ✅ 已实现 |
| `/{id}/statistics` | GET | 获取任务统计信息 | ✅ 已实现 |

**查询参数**:
- `taskName`: 任务名称（模糊查询）
- `taskCode`: 任务编码（模糊查询）
- `etlType`: ETL类型
- `executorType`: 执行器类型
- `status`: 状态
- `sourceDatasourceId`: 源数据源ID
- `targetDatasourceId`: 目标数据源ID

### 2. 字段映射管理 (ETLFieldMappingViewSet)
**路由前缀**: `/api/dataetl/field-mappings`

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/` | GET | 获取字段映射列表 | ✅ 已实现 |
| `/` | POST | 创建字段映射 | ✅ 已实现 |
| `/{id}` | GET | 获取字段映射详情 | ✅ 已实现 |
| `/{id}` | PUT | 更新字段映射 | ✅ 已实现 |
| `/{id}` | DELETE | 删除字段映射 | ✅ 已实现 |
| `/batch` | POST | 批量创建字段映射 | ✅ 已实现 |

**查询参数**:
- `taskId`: 任务ID
- `sourceFieldName`: 源字段名（模糊查询）
- `targetFieldName`: 目标字段名（模糊查询）

### 3. 执行日志管理 (ETLExecutionLogViewSet)
**路由前缀**: `/api/dataetl/execution-logs`

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/` | GET | 获取执行日志列表 | ✅ 已实现 |
| `/{id}/detail` | GET | 获取执行日志详情 | ✅ 已实现 |
| `/{id}/progress` | GET | 获取执行进度（实时） | ✅ 已实现 |
| `/{id}/cancel` | POST | 取消执行 | ✅ 已实现 |

**查询参数**:
- `taskId`: 任务ID
- `executionId`: 执行ID
- `status`: 执行状态
- `triggerType`: 触发方式
- `executedBy`: 执行者

### 4. 水印管理 (ETLWatermarkViewSet)
**路由前缀**: `/api/dataetl/watermarks`

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/` | GET | 获取水印列表 | ✅ 已实现 |
| `/task/{task_id}` | GET | 获取任务的最新水印值 | ✅ 已实现 |

### 5. 任务模板管理 (ETLTaskTemplateViewSet)
**路由前缀**: `/api/dataetl/templates`

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/` | GET | 获取模板列表 | ✅ 已实现 |
| `/` | POST | 创建模板 | ✅ 已实现 |
| `/{id}` | GET | 获取模板详情 | ✅ 已实现 |
| `/{id}` | PUT | 更新模板 | ✅ 已实现 |
| `/{id}` | DELETE | 删除模板 | ✅ 已实现 |
| `/system` | GET | 获取系统模板列表 | ✅ 已实现 |
| `/user` | GET | 获取用户模板列表 | ✅ 已实现 |
| `/create-task` | POST | 从模板创建任务 | ✅ 已实现 |
| `/{id}/increment-usage` | POST | 增加模板使用次数 | ✅ 已实现 |

### 6. 质检规则管理 (ETLQualityRuleViewSet)
**路由前缀**: `/api/dataetl/quality-rules`

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/` | GET | 获取质检规则列表 | ✅ 已实现 |
| `/` | POST | 创建质检规则 | ✅ 已实现 |
| `/{id}` | GET | 获取质检规则详情 | ✅ 已实现 |
| `/{id}` | PUT | 更新质检规则 | ✅ 已实现 |
| `/{id}` | DELETE | 删除质检规则 | ✅ 已实现 |
| `/test` | POST | 测试质检规则 | ✅ 已实现 |
| `/{id}/toggle` | POST | 切换规则启用状态 | ✅ 已实现 |

### 7. 质检结果管理 (ETLQualityResultViewSet)
**路由前缀**: `/api/dataetl/quality-results`

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/` | GET | 获取质检结果列表 | ✅ 已实现 |
| `/execution/{execution_id}` | GET | 获取指定执行的质检结果 | ✅ 已实现 |

### 8. 执行进度管理 (ETLExecutionProgressViewSet)
**路由前缀**: `/api/dataetl/execution-progress`

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/` | GET | 获取执行进度列表 | ✅ 已实现 |

---

## 二、前端页面实现分析

### 1. 已实现页面 ✅

#### [index.vue](frontend/src/views/data/etl/index.vue) - 任务列表页
**功能完整度**: ⭐⭐⭐⭐⭐ (95%)

✅ **已实现功能**:
- 任务列表展示（表格）
- 搜索筛选（任务名称、编码、类型、执行器、状态）
- 新增/修改/删除/详情/执行操作
- 版本管理（创建快照、查看版本列表、回滚）
- 分页支持
- 执行器类型/任务类型标签展示

⚠️ **存在问题**:
1. 前端使用 `taskType` 字段，但后端返回的是 `etlType`
2. 路由跳转使用 `/data-etl/task/create` 和 `/data-etl/task/edit/:id`，但实际组件是 `task-detail.vue`
3. API 函数未定义（引用了但不存在）

**API依赖**:
```javascript
import {
  listETLTask,          // ❌ 未定义
  getETLTask,           // ❌ 未定义
  delETLTask,           // ❌ 未定义
  executeETLTask,       // ❌ 未定义
  createETLTaskVersion, // ❌ 未定义
  listETLTaskVersion,   // ❌ 未定义
  rollbackETLTaskVersion // ❌ 未定义
} from '@/api/data/etl'
```

---

#### [task-detail.vue](frontend/src/views/data/etl/task-detail.vue) - 任务详情/编辑页
**功能完整度**: ⭐⭐⭐⭐ (80%)

✅ **已实现功能**:
- 任务基本信息表单
- 数据源选择（源/目标）
- 表单验证规则
- 新增/编辑模式
- 执行任务按钮
- 返回列表功能

⚠️ **存在问题**:
1. 同样使用 `taskType` 而非 `etlType`
2. 表单字段不完整，缺少执行器配置
3. 没有场景选择功能
4. 没有字段映射配置
5. API 函数未定义

**API依赖**:
```javascript
import {
  getETLTask,      // ❌ 未定义
  addETLTask,      // ❌ 未定义
  updateETLTask,   // ❌ 未定义
  executeETLTask   // ❌ 未定义
} from '@/api/data/etl'
```

---

#### [execution-log.vue](frontend/src/views/data/etl/execution-log.vue) - 执行日志页
**功能完整度**: ⭐⭐⭐⭐⭐ (90%)

✅ **已实现功能**:
- 执行日志列表展示
- 筛选功能（任务、执行ID、状态、触发方式、执行者）
- 执行状态标签展示
- 详情查看
- 错误信息查看

⚠️ **存在问题**:
1. API 函数未定义
2. 缺少实时进度查看功能
3. 缺少取消执行功能

**API依赖**:
```javascript
import {
  listETLTaskSimple,      // ❌ 未定义
  listETLExecutionLog,    // ❌ 未定义
  getETLExecutionLogDetail // ❌ 未定义
} from '@/api/data/etl'
```

---

#### [field-mapping.vue](frontend/src/views/data/etl/field-mapping.vue) - 字段映射页
**功能完整度**: ⭐⭐⭐⭐⭐ (95%)

✅ **已实现功能**:
- 字段映射列表
- 新增/修改/删除操作
- 筛选功能
- 表单验证
- 批量删除

⚠️ **存在问题**:
1. API 函数未定义
2. 没有批量创建功能

**API依赖**:
```javascript
import { listETLTaskSimple } from '@/api/data/etl' // ❌ 未定义
import {
  listETLFieldMapping,    // ❌ 未定义
  getETLFieldMapping,     // ❌ 未定义
  addETLFieldMapping,     // ❌ 未定义
  updateETLFieldMapping,  // ❌ 未定义
  delETLFieldMapping      // ❌ 未定义
} from '@/api/data/etl'
```

---

### 2. 组件实现情况 ✅

#### 已实现的组件（在 components 目录下）:
- ✅ `BasicInfoForm.vue` - 基本信息表单
- ✅ `SourceTargetForm.vue` - 源目标表单
- ✅ `SQLConfigForm.vue` - SQL配置表单
- ✅ `DataXExecutorConfig.vue` - DataX执行器配置
- ✅ `MockExecutorConfig.vue` - 模拟执行器配置
- ✅ `ScenarioSelector.vue` - 场景选择器
- ✅ `DependencyConfig.vue` - 依赖配置
- ✅ `ExecutionMonitor.vue` - 执行监控
- ✅ `SimplifiedWizard.vue` - 简化向导
- ✅ 场景配置组件（scenarios目录）

⚠️ **问题**:
这些组件已经实现，但在主页面中没有使用！

---

## 三、关键问题汇总 🔥

### 🔴 严重问题

#### 1. 前端API文件完全为空
**文件**: [frontend/src/api/data/etl.js](frontend/src/api/data/etl.js)
**状态**: ❌ **空文件**

**影响**: 所有页面都无法正常工作，因为API调用全部失败

**需要定义的API函数**:
```javascript
// ==================== ETL任务管理 ====================
export function listETLTask(query) { }
export function getETLTask(id) { }
export function addETLTask(data) { }
export function updateETLTask(data) { }
export function delETLTask(id) { }
export function listETLTaskSimple() { }
export function executeETLTask(id) { }

// ==================== 任务版本管理 ====================
export function createETLTaskVersion(id, data) { }
export function listETLTaskVersion(id) { }
export function rollbackETLTaskVersion(id, data) { }

// ==================== 字段映射管理 ====================
export function listETLFieldMapping(query) { }
export function getETLFieldMapping(id) { }
export function addETLFieldMapping(data) { }
export function updateETLFieldMapping(data) { }
export function delETLFieldMapping(id) { }

// ==================== 执行日志管理 ====================
export function listETLExecutionLog(query) { }
export function getETLExecutionLogDetail(id) { }
export function cancelETLExecution(id) { }
export function getETLExecutionProgress(id) { }

// ==================== 其他 ====================
export function validateETLConfig(id) { }
export function generateDataXConfig(id, params) { }
export function dryRunETLTask(id) { }
```

---

#### 2. 路由配置错误
**文件**: [frontend/src/router/menu.js](frontend/src/router/menu.js)

**问题**:
```javascript
// 第261行 - 错误的路由配置
{
    path: 'task/edit/:id',
    component: () => import('@/views/data/etl/detail'),  // ❌ detail.vue 已删除
    name: 'DataETLTaskEdit',
    // ...
}
```

**修复方案**:
```javascript
{
    path: 'task/edit/:id',
    component: () => import('@/views/data/etl/task-detail'),  // ✅ 正确
    name: 'DataETLTaskEdit',
    // ...
}
```

---

#### 3. 字段命名不一致
**问题**: 前端使用 `taskType`，后端使用 `etlType`

**前端代码** (多处):
```javascript
<el-select v-model="queryParams.taskType">  // ❌ 错误
<el-option label="数据集成" value="data_integration" />
```

**后端返回**:
```json
{
  "etlType": "data_integration"  // ✅ 正确字段名
}
```

**修复方案**:
统一使用 `etlType`，或者在前端做字段映射

---

### 🟡 中等问题

#### 4. TaskForm组件未充分利用
**问题**: [task-detail.vue](frontend/src/views/data/etl/task-detail.vue) 中只使用了简单的 TaskForm 组件，但 components 目录下有丰富的组件未使用:

- `ScenarioSelector.vue` - 场景选择
- `DataXExecutorConfig.vue` - DataX配置
- `MockExecutorConfig.vue` - Mock配置
- `DependencyConfig.vue` - 任务依赖
- `ExecutionMonitor.vue` - 执行监控

**建议**: 根据选择的执行器类型动态展示配置组件

---

#### 5. 缺少质检和模板管理页面
**后端已实现**: 质检规则、质检结果、任务模板管理
**前端缺失**: 对应的管理页面

**需要新增的页面**:
- `quality-rule.vue` - 质检规则管理
- `quality-result.vue` - 质检结果查看
- `template.vue` - 任务模板管理

---

#### 6. 实时进度和监控功能缺失
**后端支持**:
- `GET /execution-progress` - 执行进度
- `GET /execution-logs/{id}/progress` - 实时进度

**前端缺失**:
- WebSocket或轮询机制获取实时进度
- `ExecutionMonitor.vue` 组件未被使用

---

### 🟢 轻微问题

#### 7. 数据字段映射不完整
**后端字段** → **前端显示**:
- ✅ `sourceDatasourceName` → 已显示
- ✅ `targetDatasourceName` → 已显示
- ✅ `sourceTableName` → 已显示
- ⚠️ `executorParams` → 显示为JSON字符串，可以格式化优化

---

## 四、修复优先级建议

### P0 - 立即修复（阻塞功能）
1. ✅ **创建 frontend/src/api/data/etl.js 文件，定义所有API函数**
2. ✅ **修复路由配置中的 detail.vue → task-detail.vue**
3. ✅ **统一字段命名（taskType → etlType）**

### P1 - 高优先级（影响体验）
4. ✅ **完善 task-detail.vue，集成更多组件**
5. ✅ **添加场景选择和执行器配置功能**
6. ✅ **添加字段映射配置功能**

### P2 - 中优先级（功能完善）
7. ✅ **实现实时进度监控**
8. ✅ **新增质检管理页面**
9. ✅ **新增模板管理页面**

### P3 - 低优先级（优化体验）
10. ✅ **优化 executorParams 展示格式**
11. ✅ **添加数据预览功能**
12. ✅ **添加批量操作优化**

---

## 五、数据字段对照表

### ETL任务对象
| 后端字段 (Serializer) | 前端字段 | 类型 | 说明 |
|----------------------|---------|------|------|
| `taskId` | `taskId` | Integer | 任务ID |
| `taskName` | `taskName` | String | 任务名称 |
| `taskCode` | `taskCode` | String | 任务编码 |
| `description` | `description` | String | 描述 |
| `etlType` | `taskType` ⚠️ | String | **命名不一致** |
| `executorType` | `executorType` | String | 执行器类型 |
| `executeStrategy` | `executeStrategy` | String | 执行策略 |
| `sourceDatasourceId` | `sourceDatasourceId` | Integer | 源数据源ID |
| `sourceDatasourceName` | `sourceDatasourceName` | String | 源数据源名称 |
| `targetDatasourceId` | `targetDatasourceId` | Integer | 目标数据源ID |
| `targetDatasourceName` | `targetDatasourceName` | String | 目标数据源名称 |
| `sourceTableId` | `sourceTableId` | Integer | 源表ID |
| `sourceTableName` | `sourceTableName` | String | 源表名称 |
| `targetTable` | `targetTable` | String | 目标表名 |
| `sqlConfig` | `sqlConfig` | String | SQL配置 |
| `executorParams` | `executorParams` | JSON | 执行器参数 |
| `status` | `status` | String | 状态 |
| `remark` | `remark` | String | 备注 |

### 字段映射对象
| 后端字段 | 前端字段 | 类型 | 说明 |
|---------|---------|------|------|
| `mappingId` | `mappingId` | Integer | 映射ID |
| `taskId` | `taskId` | Integer | 任务ID |
| `sourceFieldName` | `sourceFieldName` | String | 源字段名 |
| `targetFieldName` | `targetFieldName` | String | 目标字段名 |
| `transformRule` | `transformRule` | String | 转换规则 |
| `cleanRule` | `cleanRule` | String | 清洗规则 |
| `dataType` | `dataType` | String | 数据类型 |
| `isPrimaryKey` | `isPrimaryKey` | Boolean | 是否主键 |
| `sortOrder` | `sortOrder` | Integer | 排序 |
| `remark` | `remark` | String | 备注 |

### 执行日志对象
| 后端字段 | 前端字段 | 类型 | 说明 |
|---------|---------|------|------|
| `logId` | `logId` | Integer | 日志ID |
| `executionId` | `executionId` | String | 执行ID |
| `taskId` | `taskId` | Integer | 任务ID |
| `taskName` | `taskName` | String | 任务名称 |
| `status` | `status` | String | 执行状态 |
| `triggerType` | `triggerType` | String | 触发方式 |
| `totalRows` | `totalRows` | Long | 总行数 |
| `successRows` | `successRows` | Long | 成功行数 |
| `failedRows` | `failedRows` | Long | 失败行数 |
| `durationSeconds` | `durationSeconds` | Integer | 执行时长(秒) |
| `startTime` | `startTime` | DateTime | 开始时间 |
| `endTime` | `endTime` | DateTime | 结束时间 |
| `executedBy` | `executedBy` | String | 执行者 |
| `errorMessage` | `errorMessage` | String | 错误信息 |

---

## 六、下一步行动计划

### 立即执行（本次会话）
1. ✅ 创建 `frontend/src/api/data/etl.js` 文件
2. ✅ 修复路由配置
3. ✅ 统一字段命名

### 后续优化
4. 完善任务详情页面，集成更多组件
5. 添加质检和模板管理页面
6. 实现实时进度监控

---

## 七、测试建议

### 单元测试
- ✅ 测试每个API接口的请求/响应格式
- ✅ 测试字段映射是否正确

### 集成测试
- ✅ 测试任务创建流程
- ✅ 测试任务执行流程
- ✅ 测试字段映射流程

### E2E测试
- ✅ 完整的业务流程测试
- ✅ 边界情况测试
- ✅ 错误处理测试

---

**报告结论**:
后端API功能完整且设计合理，前端页面框架已搭建完成，但**缺少关键的API定义文件**和存在一些配置错误。修复P0优先级问题后，系统即可正常运行。
