# 需求目录
**目标**
  实现一个数据资产管理平台，包含以下模块
  - 数据资产管理 aasset
  - 数据ETL开发 etl
  - 数据质量管理 quality
  - 任务运维 task
  - 数据服务 service


## 数据资产
**背景**
  要实现数据ETL模块，前置任务应该是要先知道有哪些数据，数据的去向。


## 数据ETL模块
**背景**
  基于数据仓库的背景下，设计数据集成模块，基于数仓的数据集成功能在数据的不同阶段应该选择合适的集成方式，例如数据从外部数据源集成到数仓平台（例如hive）后，后续阶段的数据处理过程均可以基于hive/spark-sql来开发，而外部数据集成例如数据库同步到hive可使用datax作为实现工具，更新一下这一步骤的设计。

  数据ETL模块的具体场景：
  - 1：数据从业务库集成到STG； 
  - 2：数据从STG集成到ODS； 
  - 3：数据在数仓层的汇聚，转换，计算等； 
  - 4：数据从数仓层同步到具体的业务数据库中；
  - 5：数据库之间的互相同步

**功能描述**
前端优化一下，我觉得现在前端针对这五种场景的操作有点繁琐，应该简洁些，直接告知用户支持的场景，然后当用户选择其中一种场景后，除了必要的配置需要用户来操作其他的应该尽量简单，例如当用户选择数据从业务库集成到STG时，只需要选择数据源，表信息以及一些过滤条件/增量还是全量等信息，然后点击开始同步，然后就可以看到同步进度了，这样用户操作起来会比较简单



## 任务运维
**背景**
  管理各类数据处理任务，包括数据ETL中的数据集成任务，数据清洗转换等处理任务

## 数据质量
**背景**
  允许添加一些数据质量的检查任务，对错误信息预警


## 数据服务
**背景**
  提供数据服务，支持数据查询，对外数据接口，发布BI报表
